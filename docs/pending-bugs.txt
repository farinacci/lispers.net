
- Have lig get back entire locator-set like xTRs do with decent-nat configured.

- python3 on MacOS is not registering with decent mapping system.

- RLOC-probing not working for multicast EIDs. Even when both unicast and multicast
  RLOcs are used. The (S-EID, G-EID) are not placed in the rloc-probe-list. Therefore,
  telemetry statistics are not reported for the RLE RLOCs.

